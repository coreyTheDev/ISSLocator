# Uncomment this line to define a global platform for your project
# platform :ios, ’10.0’

use_frameworks!

target ‘ISSLocator’ do
  # Comment this line if you're not using Swift and don't want to use dynamic 
pod 'Alamofire','~> 4.0.1'
pod 'SwiftyJSON'

  # Pods for ISSLocator

end
